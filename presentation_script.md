# 🎤 B1-2 트러블슈팅 과제 발표/면접 대본 (1:1 대응)

> **💡 사용 가이드**
> 본 대본은 `B1-2-Evalution.md`의 평가 항목 순서대로 구성되었습니다. 면접관의 질문에 답변하거나, 프레젠테이션을 진행할 때 자연스럽게 읽을 수 있도록 구어체로 작성되었습니다. 본인의 입에 맞게 조금씩 수정해서 사용하세요!

---

## 🎙️ 오프닝
"안녕하십니까, B1-2 리눅스 트러블슈팅 과제를 수행한 [본인 이름]입니다. 이번 미션에서 저는 단순히 에러를 끄는 것을 넘어, OS 모니터링 도구의 한계를 극복하고 시스템의 숨겨진 아키텍처를 역추적하는 데 집중했습니다. 지금부터 OOM, CPU Spike, Deadlock 세 가지 장애에 대한 분석 과정과 근본적 해결책을 발표하겠습니다."

---

## 항목 1. 결과물 및 객관적 증거 (팩트 기반 보고)

**[OOM 방어]**
"첫 번째로 OOM 장애입니다. 로그를 분석한 결과, 메모리가 50MB에서 75MB로 선형 증가하자 앱 내부의 `MemoryGuard`가 시스템 붕괴를 막기 위해 스스로 프로세스를 사살(SIGTERM)하는 것을 확인했습니다. 저는 `MEMORY_LIMIT`을 60MB에서 512MB로 대폭 상향하는 조치를 취했고, 그 결과 앱이 터지는 대신 내부의 `Cache Flush` 메커니즘이 정상 발동하여 힙 메모리를 25MB로 비워내며 180초 이상 안정적으로 생존하는 것을 Before & After 테스트로 완벽히 증명했습니다."

**[CPU 방어]**
"두 번째는 CPU 과점유입니다. 부하가 51.37%를 돌파하자 앱 내부의 `Watchdog`이 발동해 자결하는 현상을 포착했습니다. 이를 막기 위해 컨테이너의 `CPU_MAX_OCCUPY` 파라미터를 80%에서 40%로 하향 제한했고, 그 결과 부하가 40.00% 정점을 찍은 후 쿨다운 상태로 진입해 타임아웃까지 무사히 생존함을 확인했습니다."

**[Deadlock 방어]**
"마지막으로 데드락입니다. 관제 로그 상에서 CPU와 메모리 변화량이 0으로 수렴하며 앱이 살아있지만 무응답 상태인 'Silent Hang'을 식별했습니다. 앱 로그를 뜯어보니 100개의 스레드가 락을 물고 물리는 현상이 있어, `MULTI_THREAD_ENABLE` 옵션을 0으로 설정해 순차 실행을 강제함으로써 데드락을 회피하고 60초간 정상 동작함을 확인했습니다."

---

## 항목 2. 도구 활용 및 진단 과정 (모니터링의 한계 돌파)

**[OS 도구의 한계와 하이브리드 파싱]**
"장애를 진단하는 과정에서 저는 OS 기본 도구들의 치명적인 맹점을 발견했습니다. 타겟 앱이 연산과 휴식을 짧게 반복하는 '마이크로 버스트(Micro-sleep)' 기법을 썼기 때문에, 스냅샷 방식인 `top` 명령어는 타이밍이 어긋나 부하를 0%로 오판했고, 누적 평균을 내는 `ps` 명령어는 1%대의 정상 수치로 거짓말을 했습니다. 

이를 돌파하기 위해 저는 `monitor.sh` 스크립트를 개선하여, OS 메트릭뿐만 아니라 앱이 뱉어내는 내부 로그까지 실시간으로 텍스트 파싱(`PARSING_CPU`, `PARSING_MEM`)하는 **하이브리드 관제 시스템**을 구축했습니다. 덕분에 무응답 프로세스의 징후를 가장 먼저 캐치하고, 'PID Not Found'가 아닌 앱의 '진짜 사망 사유(유언장)'를 발췌해 낼 수 있었습니다."

---

## 항목 3. 기술 원리 이해 (딥 다이브)

**[왜 앱은 스스로 자결했는가?]**
"이 앱들은 메모리나 CPU가 임계치를 넘었을 때 그냥 죽은 것이 아닙니다. 메모리 누수가 발생해 호스트 OS의 물리적 메모리까지 고갈시키면 OS의 OOM Killer가 무고한 다른 프로세스까지 죽이는 시스템 패닉이 발생합니다. CPU 역시 100%를 선점하면 전체 서버가 멈춥니다. 이를 막기 위해 앱 내부에 `MemoryGuard`와 `Watchdog`이라는 훌륭한 자체 보호 메커니즘이 존재함을 로그를 통해 기술적으로 파악했습니다."

**[교착상태(Deadlock)의 드라마틱한 원리]**
"데드락 로그는 마치 두 명의 고집불통을 보는 것과 같았습니다. 스레드 1번이 공유 메모리 A의 락을 쥔 상태(상호 배제)에서 소켓 B를 기다리고(점유 대기), 스레드 2번은 소켓 B를 쥔 채 메모리 A를 기다리는 완벽한 '원형 대기(Circular Wait)' 조건이 성립했습니다. 서로 자원을 강제로 뺏을 수 없는 비선점 조건 속에서 영원히 멈춰버린 것이죠."

---

## 항목 4. 실무 적용 및 심화 사고 (면접관 킬러 질문 방어)

**[Q1. 실제 운영 환경이라면 모니터링을 어떻게 개선할 것인가?]**
"실제 서버라면 쉘 스크립트 기반의 Polling 방식만으로는 한계가 큽니다. 프로메테우스(Prometheus)를 활용해 애플리케이션 내부에서 직접 메트릭을 Expose 하도록 Push/Pull 방식을 구성하고, 그라파나(Grafana)로 시각화하여 메모리 증가 추세선의 기울기가 급격해질 때 Slack으로 사전 얼럿(Alert)을 쏘도록 개선하겠습니다."

**[Q2. 가장 치명적인 장애와 우선순위는?]**
"가장 치명적인 것은 단연 **Deadlock(교착 상태)**입니다. OOM이나 CPU Spike는 최소한 앱이 죽거나 OS에 의해 재시작(Restart)이라도 되지만, 데드락은 프로세스가 겉으로는 '정상(Running)' 상태로 위장한 채 좀비처럼 멈춰있기 때문에 로드밸런서의 Health Check를 통과해 지속적으로 사용자 요청을 블랙홀처럼 빨아들일 위험이 큽니다. 따라서 복합 장애 발생 시, 당장 시스템 자원 여유가 있다면 **Deadlock 원인 분석 및 해결**을 1순위로 진행해야 합니다."

**[Q3. 코드 레벨에서의 근본적 개선 방안은?]**
"지금의 환경변수 조작은 회피책일 뿐입니다. 소스를 고칠 수 있다면, 
1. **OOM**: 불필요한 객체 참조를 해제하여 가비지 컬렉터(GC)가 정상 작동하게 고치고, 현재 525MB로 하드코딩된 Cache Flush 기준을 동적으로 낮추겠습니다.
2. **CPU Spike**: 무한 루프 내에 `sleep`을 적절히 주거나, I/O 대기 시 비동기(Async/NIO) 처리를 통해 CPU 점유를 낮추겠습니다.
3. **Deadlock**: 멀티스레드를 유지하되, 모든 스레드가 자원을 획득하는 순서를 강제로 통일하는 **Lock Ordering** 기법을 적용하거나, 일정 시간 락을 못 얻으면 포기하는 **Timeout Lock**을 도입하겠습니다."

---

## 항목 5. 보너스 (스케줄링 알고리즘 추론)

"마지막으로 보너스 과제인 스케줄링 알고리즘 추론입니다. 정상 동작(단일 스레드) 로그를 분석해 보니, 각 스레드가 정확히 40% 분량의 작업만 수행하고 강제로 중단(`Preempted`)되며 다음 스레드에게 기회를 넘기는 현상을 발견했습니다. 

만약 먼저 온 작업을 끝내는 **FCFS**였다면 중간에 끊기는 일 없이 100% 도달 후 교대했을 것이고, **Priority** 방식이었다면 VIP 스레드가 자원을 뺏거나 독점했을 것입니다. 골고루 시간을 나눠 가지는 이 명확한 패턴을 통해 저는 본 시스템이 **라운드 로빈(Round-Robin)** 알고리즘으로 동작함을 완벽하게 추론했습니다. 

이러한 라운드 로빈은 특정 스레드의 기아(Starvation)를 막고 응답 시간을 균일하게 보장하므로, 실시간 응답이 중요한 **대화형 웹 서버(Web Server) 아키텍처**에 매우 적합하다는 결론까지 도출했습니다."

---

## 🎬 클로징
"이번 과제를 통해 단순히 '장애가 났다, 변수를 바꿨다, 고쳤다'가 아니라, **'OS가 왜 속았는가, 앱은 왜 자결을 택했는가, 스케줄링은 어떻게 동작하는가'**라는 시스템의 깊은 이면을 들여다볼 수 있었습니다. 이상으로 발표를 마치겠습니다. 감사합니다!"
