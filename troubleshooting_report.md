# 📊 CPU_Before 시나리오 트러블슈팅 분석 보고서

이 보고서는 `agent-leak-app`의 CPU 한계 초과 시나리오(`CPU_Before`)를 분석하며 발견한 OS 모니터링 도구의 치명적 한계와, 이를 극복하기 위해 적용한 **하이브리드 관제(로그 파싱)** 기법의 타당성을 증명합니다.

> [!CAUTION]
> **Boot Spike의 함정 (오토 킬 주의)**
> 프로그램 기동 직후(0초) `top` 명령어는 초기 구동 부하(Boot Spike)로 인해 일시적으로 `80.4%`를 보고했습니다. 만약 관제 시스템이 `top` 수치에 의존하여 오토 킬(Auto-kill) 하도록 설정되어 있었다면, 서비스는 시작도 하기 전에 오탐지(False Positive)로 인해 강제 종료되었을 것입니다.

## 1. OS 기본 도구의 완벽한 패배 (스텔스 부하 증명)

앱이 의도적으로 짧게 연산하고 길게 쉬는 **마이크로 버스트(Micro-sleep)** 기법을 사용함에 따라, 리눅스 기본 모니터링 명령어들은 다음과 같은 맹점을 드러냈습니다.

- **`top`의 요동 (Polling 구조의 근본적 한계와 관찰자 효과)**
  - 외부에서 주기적으로 스냅샷을 찍는(Polling) 방식 자체의 한계입니다. 주기를 1초로 하든, 간격을 극한(0.5초)으로 좁히든(이 경우 오히려 관찰자 효과로 인해 240% 같은 왜곡된 수치 발생), 타겟 앱의 휴식/연산 타이밍과 엇갈릴 때마다 `0%`를 반환하거나 허위 수치를 뿜어내며 완전히 농락당했습니다. (`80.4% -> 0% -> 24% -> 0% -> 48%`)
- **`ps`의 무능 (누적 평균의 함정)**
  - 프로세스 생존 시간 대비 누적 평균값을 계산하는 `ps`는 앱의 긴 휴식 시간에 의해 평균치가 희석되어, 스파이크 발생 중임에도 불구하고 지속적으로 `0.6% ~ 2.0%`의 정상 수치로 오판했습니다.

## 2. 하이브리드 관제(PARSING_CPU)의 승리

OS 명령어들이 거짓말을 하는 와중에도, 앱 내부 로그를 실시간으로 긁어오도록 스크립트에 탑재한 파싱 로직(`PARSING_CPU`)은 실제 부하의 점진적 팽창을 정확하게 포착했습니다.
- `5.00% -> 14.10% -> 23.72% -> 40.20% -> 48.49%`

> [!TIP]
> **실무 적용 포인트**
> 실제 운영 서버에 침투한 악성코드나 버그 난 프로세스는 자기가 몇 초 주기로 도는지 절대 알려주지 않습니다. 외부 관제 스크립트만으로는 타겟 앱의 내부 박자를 완벽히 따라갈 수 없으므로, **내부 자가 진단 메트릭(파싱)**을 병행하는 하이브리드 방식이 필수적입니다.

## 3. 관제 시스템 경고 발동 및 유언장 파싱 성공

1. **사전 경고 발동**: `PARSING_CPU`가 40%를 돌파하고 `top`이 48.0%를 포착한 순간, 모니터링 스크립트가 선제적으로 `[WARNING: CPU High (관제 임계점)]` 경고를 정상 발생시켰습니다.
2. **Watchdog 자결**: 부하가 자체 하드코딩 임계점(50%)을 초과하자, 내부 Watchdog이 프로세스를 강제 셧다운 시켰습니다.
3. **유언장 파싱 성공**: 모니터가 PID 소멸을 감지한 직후, 단순 "PID Not Found"가 아닌 원본 로그를 뒤져 **`[CRITICAL] 프로세스 사망. 원인: [CpuWorker] CPU Threshold Violated! (54.13%)`** 라는 정확한 사망 사유를 적출해 내는 데 성공했습니다.
